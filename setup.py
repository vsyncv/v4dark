from setuptools import setup

setup(name='v4dark',
      version='0.7',
      description='super fancy discord bot',
      packages=['v4dark'],
      install_requires=['colorlog',
      'discord.py',
      'python-dotenv'
      ],
      zip_safe=False)