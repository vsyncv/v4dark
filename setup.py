from setuptools import setup

setup(name='v4dark',
      version='0.2',
      description='super fancy discord bot',
      packages=['v4dark'],
      install_requires=['colorlog',
      ],
      zip_safe=False)